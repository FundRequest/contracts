const VM = require('../index.js')

var cacheDB = new Level('./.cachedb')

// 1) not handling logic to check highest blocks
// 2) we are getting the highest block with the most PoW

const incomingBlock = new Block()
console.log(incomingBlock);
// revive the state from local storage
// const trie = new Trie()
//
// state : = [address, account]
// account := [balance, nonce, codeHash, StorageRoot]
//
// vm.state // https://github.com/ethereumjs/merkle-patricia-tree

const vm = new VM()

// if (incomingBlock.validate(   )) {
  vm.runBlock(incomingBlock, (results, err) => {

  })
// }
